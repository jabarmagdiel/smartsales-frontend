[providers]
node = "18"

[variables]
NODE_ENV = "production"
PORT = "3000"

[phases.setup]
nixPkgs = [
  "nodejs_18",
  "npm-9_x"
]

[phases.install]
cmds = [
  "npm ci --only=production"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "npm start"
